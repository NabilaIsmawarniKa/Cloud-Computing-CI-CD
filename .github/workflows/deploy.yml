name: Deploy ke Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout kode
        uses: actions/checkout@v3

      - name: Deploy website
        run: |
          rsync -av --exclude='.git' --exclude='.github' $GITHUB_WORKSPACE/mywebntahlah/ /var/www/html/